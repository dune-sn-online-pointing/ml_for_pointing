{
  "model_name": "d3_views_hp_simple_cnn",
  "output_folder": "/eos/user/e/evilla/dune/sn-tps/neural_networks",
  "data_directories": [
    "/eos/user/e/evilla/dune/sn-tps/production_es/images_es_prod_main_tick3_ch2_min2_tot3_e2p0"
  ],
  "use_matched_clusters": true,
  "allow_partial_matching": true,
  "filter_es_only": true,
  "filter_main_track_only": true,
  "dataset_parameters": {
    "train_fraction": 0.6,
    "val_fraction": 0.2,
    "test_fraction": 0.2,
    "shuffle_data": true,
    "balance_data": false,
    "max_samples": null,
    "random_seed": 42,
    "batch_pattern": "*_matched_plane*.npz"
  },
  "model_parameters": {
    "input_shape": [
      128,
      16,
      1
    ],
    "output_dim": 3,
    "hp_max_evals": 20,
    "space_options": {
      "n_conv_layers": [
        2,
        3,
        4
      ],
      "n_dense_layers": [
        2,
        3,
        4
      ],
      "n_filters": [
        32,
        64,
        128,
        256
      ],
      "kernel_size": [
        3,
        5
      ],
      "n_dense_units": [
        64,
        128,
        256,
        512
      ],
      "dropout_rate": [
        0.1,
        0.2,
        0.3,
        0.4
      ],
      "learning_rate": [
        0.0001,
        0.005
      ],
      "decay_rate": [
        0.9,
        0.95,
        0.98,
        0.999
      ],
      "batch_size": [
        64,
        128,
        256
      ]
    },
    "loss_function": "mse",
    "epochs": 100,
    "batch_size": 128,
    "early_stopping": {
      "monitor": "val_loss",
      "patience": 15,
      "restore_best_weights": true
    }
  },
  "save_outputs": {
    "save_model": true,
    "save_history": true,
    "save_predictions": true,
    "save_metrics": true
  },
  "training_notes": {
    "date": "2025-11-04",
    "version": "v2_3plane_matched_clusters",
    "changes_from_previous": [
      "Updated to use matched clusters with match_id linking",
      "Allow partial matching (samples with 1, 2, or 3 planes)",
      "Added dropout_rate to search space: [0.1, 0.2, 0.3, 0.4]",
      "Increased hp_max_evals from 10 to 20",
      "Using matched clusters from new pipeline (2.0 MeV threshold)"
    ],
    "rationale": "Leverage 3-view information when available via matched cluster IDs. Allow partial matching to use all available data."
  }
}